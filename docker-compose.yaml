version: '3'
services:
  base:
    build:
      context: ./
      dockerfile: ./Dockerfile
    working_dir: /workplace
    volumes:
      - "${HOME}/typedflow:/workplace"
      - "${HOME}/typedflow/typedflow:/usr/local/lib/python3.8/site-packages/typedflow:ro"
